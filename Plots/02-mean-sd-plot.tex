\documentclass[border=3]{standalone}
\usepackage{pgfplots}
\usepackage{tikz}

%\pgfplotsset{compat=1.7}

\begin{document}
\begin{tikzpicture}
  \begin{axis}[grid=major,
        xmin=0,
        xmax = 3,
        ymin = 0,
        ymax = 90,
       % axis x line = bottom,
       % axis y line = left
       ]
    \addplot+[smooth,
    error bars/.cd,
    y dir=both,
    y explicit
    ]
    table[x index=0, y index=1, y error index=2]
    {02-mean-sd-plot.table}; % simple space-delimited data file
  \end{axis}
\end{tikzpicture}
\end{document}
